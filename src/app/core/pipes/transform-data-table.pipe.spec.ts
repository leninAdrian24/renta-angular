import { TransformDataTablePipe } from './transform-data-table.pipe';

describe('TransformDataTablePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDataTablePipe();
    expect(pipe).toBeTruthy();
  });
});
